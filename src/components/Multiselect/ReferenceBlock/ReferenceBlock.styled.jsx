import styled from "styled-components";

export const StyledReferenceBlock = styled.div`
    position: absolute;
    pointer-events: none;
    bottom: 0;
    opacity: 0;
    display: flex;
    align-items: center;
`;